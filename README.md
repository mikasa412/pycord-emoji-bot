# pycord-emoji-bot
simple pycord bot to help me gain skill - reacts with custom emoji to a message you choose

uses a .env file with:
- TOKEN: your discord bot's token

uses a stats.json file configured like:
```json
{
    "template": {}
}
```